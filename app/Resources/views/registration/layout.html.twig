{% extends '::base.html.twig' %}

{% block body %}
    <div class="container mt-5 mb-5" style="max-width: 500px;">
        <h3>Inscriptions {{ date('now')|date('Y') }} - {{ date('now')|date('Y') + 1 }}</h3>

        {% block content %}{% endblock %}
    </div>

    <div class="mt-5 pt-3 pb-3" style="background: #f9f9f9; color: #afafaf">
        <div class="container" style="max-width: 500px;">
            Un prôblème ? Contacte nicolas.ka sur Facebook ❤︎
        </div>
    </div>
{% endblock %}
